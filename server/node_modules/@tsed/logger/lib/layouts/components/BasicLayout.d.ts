import { BaseLayout } from "@tsed/logger/lib/layouts/class/BaseLayout";
import { LogEvent } from "@tsed/logger/lib/core/LogEvent";
export declare class BasicLayout extends BaseLayout {
    /**
     * BasicLayout is a simple layouts for storing the logs. The logs are stored
     * in following format:
     * <pre>
     * [startTime] [logLevel] categoryName - message\n
     * </pre>
     *
     * @author Stephan Strittmatter
     */
    transform(loggingEvent: LogEvent, timezoneOffset?: number): string;
}
