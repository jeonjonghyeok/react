import { BeforeRoutesInit, OnInit, PlatformApplication } from "@tsed/passport/lib/node_modules/@tsed/common";
import { PassportSerializerService } from "@tsed/passport/lib/services/PassportSerializerService";
import { ProtocolsService } from "@tsed/passport/lib/services/ProtocolsService";
/**
 * @ignore
 */
export declare class PassportModule implements OnInit, BeforeRoutesInit {
    private app;
    private protocolsService;
    private passportSerializer;
    userProperty: string;
    pauseStream: boolean;
    platformName: string;
    constructor(app: PlatformApplication, protocolsService: ProtocolsService, passportSerializer: PassportSerializerService);
    $onInit(): Promise<any> | void;
    $beforeRoutesInit(): void | Promise<any>;
}
