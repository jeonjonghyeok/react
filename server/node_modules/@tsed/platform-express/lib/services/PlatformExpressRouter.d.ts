import { PlatformHandler, PlatformRouter, PlatformStaticsOptions } from "@tsed/platform-express/lib/services/node_modules/@tsed/common";
import { Configuration } from "@tsed/platform-express/lib/services/node_modules/@tsed/di";
import * as Express from "express";
import { RouterOptions } from "express";
declare global {
    namespace TsED {
        interface Router extends Express.Router {
        }
    }
}
/**
 * @platform
 * @express
 */
export declare class PlatformExpressRouter extends PlatformRouter<Express.Router> {
    constructor(platform: PlatformHandler, configuration: Configuration, routerOptions?: Partial<RouterOptions>);
    statics(endpoint: string, options: PlatformStaticsOptions): this;
}
