import { PlatformMulterField, PlatformMulterSettings } from "@tsed/common/lib/config";
import { IMiddleware } from "@tsed/common/lib/mvc/interfaces/IMiddleware";
import { Context } from "@tsed/common/lib/platform/decorators/context";
import { PlatformApplication } from "@tsed/common/lib/platform/services/PlatformApplication";
export interface MulterInputOptions {
    fields: PlatformMulterField[];
}
/**
 * @middleware
 */
export declare class PlatformMulterMiddleware implements IMiddleware {
    settings: PlatformMulterSettings;
    app: PlatformApplication;
    use(ctx: Context): Promise<any>;
    protected getFields(conf: MulterInputOptions): {
        name: string;
        maxCount: number | undefined;
    }[];
}
