import { Configuration } from "@tsed/common/lib/platform/middlewares/node_modules/@tsed/di";
import { IMiddleware } from "@tsed/common/lib/mvc";
import { Context } from "@tsed/common/lib/platform/decorators/context";
/**
 * @middleware
 */
export declare class GlobalAcceptMimesMiddleware implements IMiddleware {
    private configuration;
    constructor(configuration: Configuration);
    use(ctx: Context): void;
}
