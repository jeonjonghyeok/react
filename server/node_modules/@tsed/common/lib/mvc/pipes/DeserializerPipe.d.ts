import { IPipe, ParamMetadata } from "@tsed/common/lib/mvc/models/ParamMetadata";
import { ConverterService } from "@tsed/common/lib/mvc/services/ConverterService";
export declare class DeserializerPipe implements IPipe {
    private converterService;
    constructor(converterService: ConverterService);
    transform(value: any, param: ParamMetadata): any;
}
