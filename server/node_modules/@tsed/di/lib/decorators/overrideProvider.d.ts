import { Type } from "@tsed/di/lib/decorators/node_modules/@tsed/core";
/**
 * Override a provider which is already registered in ProviderRegistry.
 * @returns {Function}
 * @decorators
 * @param originalProvider
 */
export declare function OverrideProvider(originalProvider: Type<any>): Function;
