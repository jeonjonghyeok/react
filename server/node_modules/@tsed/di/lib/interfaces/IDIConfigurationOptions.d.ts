import { IDIResolver } from "@tsed/di/lib/interfaces/IDIResolver";
import { ProviderScope } from "@tsed/di/lib/interfaces/ProviderScope";
import { TokenProvider } from "@tsed/di/lib/interfaces/TokenProvider";
declare global {
    namespace TsED {
        interface Configuration {
            scopes: {
                [key: string]: ProviderScope;
            };
            /**
             * Define a list of resolvers (it can be an external DI).
             */
            resolvers: IDIResolver[];
            /**
             * Define dependencies to build the provider
             */
            imports: TokenProvider[];
            [key: string]: any;
        }
    }
}
export interface IDIConfigurationOptions extends TsED.Configuration {
}
