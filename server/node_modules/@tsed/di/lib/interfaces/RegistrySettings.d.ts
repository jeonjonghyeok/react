import { Registry } from "@tsed/di/lib/interfaces/node_modules/@tsed/core";
import { Provider } from "@tsed/di/lib/class/Provider";
import { IProvider } from "@tsed/di/lib/interfaces/IProvider";
export interface RegistrySettings {
    registry: Registry<Provider<any>, IProvider<any>>;
    injectable?: boolean;
    /**
     *
     * @param provider
     * @param {Map<string | Function, any>} locals
     * @param deps
     */
    onInvoke?(provider: Provider<any>, locals: Map<string | Function, any>, deps: any[]): void;
}
