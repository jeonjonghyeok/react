import { ProviderScope } from "@tsed/di/lib/interfaces/ProviderScope";
import { TokenProvider } from "@tsed/di/lib/interfaces/TokenProvider";
export interface InvokeOptions<T = any> {
    deps: any[];
    imports: any[];
    parent?: TokenProvider;
    scope: ProviderScope;
    useScope: boolean;
    rebuild?: boolean;
}
