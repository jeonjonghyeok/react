import { IProvider } from "@tsed/di/lib/interfaces/IProvider";
export declare function mapConfiguration(config: any): {
    endpoint?: string;
    values: any[];
}[];
export declare function importComponents(config: any, excludes: string[]): Promise<Partial<IProvider<any>>[]>;
