import { OS3Header } from "@tsed/schema/lib/interfaces/node_modules/@tsed/openspec";
export interface JsonHeader extends OS3Header {
}
export interface JsonHeaders {
    [key: string]: number | string | (JsonHeader & {
        type?: string;
        value?: string | number;
    });
}
