import { BeforeRoutesInit, Configuration, InjectorService, OnReady, PlatformApplication } from "@tsed/swagger/lib/node_modules/@tsed/common";
import { SwaggerSettings } from "@tsed/swagger/lib/interfaces";
import { SwaggerService } from "@tsed/swagger/lib/services/SwaggerService";
/**
 * @ignore
 */
export declare class SwaggerModule implements BeforeRoutesInit, OnReady {
    injector: InjectorService;
    app: PlatformApplication;
    configuration: Configuration;
    swaggerService: SwaggerService;
    private loaded;
    get settings(): SwaggerSettings[];
    /**
     *
     */
    $beforeRoutesInit(): void;
    $onRoutesInit(): void;
    $onReady(): void;
    private getUrls;
    /**
     *
     * @param conf
     * @param urls
     */
    private createRouter;
    private middlewareSwaggerJson;
}
