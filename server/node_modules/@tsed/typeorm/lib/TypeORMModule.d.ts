import { Configuration, OnDestroy } from "@tsed/typeorm/lib/node_modules/@tsed/common";
import { TypeORMService } from "@tsed/typeorm/lib/services/TypeORMService";
export declare class TypeORMModule implements OnDestroy {
    private typeORMService;
    private settings;
    constructor(configuration: Configuration, typeORMService: TypeORMService);
    init(): Promise<any>;
    $onDestroy(): Promise<any> | void;
}
